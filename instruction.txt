1. Развернуть локально в docker-compose;
    - postgres
    - nats JetStream

2. Собрать + запустить сервис

3. Посылаем в канал мусор (скрипт go run my_spam_publisher.go)

4. проверяем, что в БД ничего нет (docker exec -it my_postgres bash; psql -U postgres -d testdb; SELECT COUNT(*) FROM WB_Order;)

4. Сервис падает

5. Посылаем в канал валидные значения (скрипт go run my_true_publisher.go)

6. Восстанавливаем сервис (данные не пропали, а успешно записались в кэш и БД)

7. Проверяем, что в БД появились 2 записи

7. Пробуем получить по UID запись, которая есть в БД

8. Пробуем получить по UID запись, которой нет в БД